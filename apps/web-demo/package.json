{
  "name": "@loam/web-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Hosted demo for Loam — single page, single Play button. Stage 3: hard-coded Dm9 → Gmaj7 vamp through the modular engine.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@loam/core": "workspace:*",
    "@loam/synth-tone": "workspace:*",
    "tone": "^14.8.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
